# Might as well
--sort-files

# As per Vim: Ignore case, unless some character in the search string is in upper case
--smart-case

# I really can't stomach ack's default bright yellow inverse.
# This is the same as grep --color.
--color-match=bold red

# CMake generates *.make files
--type-add=make:ext:make

