# .dotfiles/inputrc
# Symlink me as ~/.inputrc

set input-meta on
set output-meta on

set completion-ignore-case On
set match-hidden-files Off
set editing-mode vi

# UpArrow/DownArrow history-search-backward/forward
# The up and down arrows still behave as normal when no characters have been typed,
# but behave the same as in Vim once leading characters have been typed: searching back through the history.
"\e[A": history-search-backward
"\e[B": history-search-forward

